# Copy to infra/.env before `docker compose up -d` on 192.168.1.9. NEVER commit .env.
POSTGRES_DB=gravelled
POSTGRES_USER=gravelled
POSTGRES_PASSWORD=CHANGEME-choose-a-strong-password
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=CHANGEME-choose-an-admin-password
