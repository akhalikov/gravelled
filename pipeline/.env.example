# Copy to pipeline/.env and fill in. NEVER commit .env (see .gitignore).
# Connection string for the Postgres on the home server (192.168.1.9).
DATABASE_URL=postgresql://gravelled:CHANGEME@192.168.1.9:5432/gravelled

# Optional: override the Garmin token directory (default: ~/.garminconnect)
# GARMINTOKENS=~/.garminconnect
